# Mutation batch 46–60

This batch adds fifteen authored mutations to the production pool, preserving the four-category split and full-vector trade-off rule. Each resource has bounded adaptation, compute, alignment, integrity, and guard values, plus player-facing `Counterplay:` text.

The balance sheet records direct vectors and assumptions in `docs/content/mutation-balance.md`. `game/tests/test_mutation_batch_46_60.gd` verifies all fifteen ids, counterplay notes, non-dominant vectors, exact pool size, and five curated combo outcomes.
