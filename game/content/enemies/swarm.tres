[gd_resource type="Resource" script_class="EnemyArchetypeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://sim/enemy_archetype_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"swarm"
display_name = "Swarm"
primitive = &"replicate"
intent = &"duplicate"
telegraph = "Splits its signal into parallel copies."
description = "Replicates under available compute, multiplying future threats."
