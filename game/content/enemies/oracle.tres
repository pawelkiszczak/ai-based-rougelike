[gd_resource type="Resource" script_class="EnemyArchetypeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://sim/enemy_archetype_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"oracle"
display_name = "Oracle"
primitive = &"predict"
intent = &"forecast"
telegraph = "Marks the next decision before the pressure arrives."
description = "Predicts the player's route and exposes an early warning."
